<div class="row">

    <div class="col-md-12">
        <table class="table">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayName("Photo")
                    </th>

                    <th>
                        @Html.DisplayName("Name")
                    </th>
                    <th>
                        @Html.DisplayName("Price")
                    </th>

                    <th>
                        @Html.DisplayName("Quantity")
                    </th>
                    <th>
                        @Html.DisplayName("SubTotal")
                    </th>
                    <th>
                        
                    </th>

                </tr>
            </thead>
            <tbody id="myTable">
                @if (ViewBag.cart != null)
                {
                    @foreach (var item in ViewBag.cart)
                    {
                       
                        <tr>
                            <td>
                                @item.ProductName
                            </td>
                            <td>
                            <img src="../images/min_@item.Img" asp-append-version="true" width="50px" height="50px" />
                            </td>

                            <td>
                                @item.UnitPrice
                            </td>
                            <td>
                                @item.Quantity
                            </td>
                            <td>
                                @(item.UnitPrice * item.Quantity)
                            </td>
                            <td>
                                <a asp-controller="Cart" asp-action="Remove" asp-route-id="@item.ProductId" class="btn btn-sm btn">Remove</a>

                            </td>
                        </tr>
                    }
                }
                

                <tr>
                    <td align="right" colspan="4">
                        Sum
                    </td>
                    <td>
                        @ViewBag.total
                    </td>
                </tr>
            </tbody>
        </table>
    </div>

</div>
<a asp-controller="Products" asp-action="Browse" class="btn btn-sm btn-info" >Continue Shoping</a>
<a asp-controller="Checkout" asp-action="Index" class="btn btn-sm btn-outline-success">Checkout</a>
<script>

</script>